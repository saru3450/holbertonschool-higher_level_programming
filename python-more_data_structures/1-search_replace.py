#!/usr/bin/python3
def search_replace(my_list, search, replace):
    mon_list = my_list.copy()
    for i in range(len(new_list_list)):
            if mon_list[i] == search:
                mon_list[i] = replace
    return mon_list
